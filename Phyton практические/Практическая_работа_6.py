# Практическая работа №6
# №2
import array
from pickle import TRUE

# №1 В массиве действительных чисел все нулевые элементы заменить на среднее арифметическое всех элементов массива. Вариант 

def n1(a:array, n:int):
    summ = 0
    for i in range (n):
        summ += a[i]
    summ /= n
    for i in range (n):
        if a[i] == 0:
            a[i] = summ
    print(a)

# №2 Дан массив целых чисел. Переписать все положительные элементы во второй массив, а остальные - в третий. 
def n2(a:array, n:int):
    positive=[]
    negative=[]
    for i in range (n):
        if a[i] > 0:
            positive.append(a[i])
        if a[i] < 0:
            negative.append(a[i])
    print(f"Позитивные числа: {positive} \nНегативные числа: {negative}")

# №3 Дан одномерный массив из 8 элементов. Заменить все элементы массива меньшие 15 их удвоенными значениями. Вывести на экран монитора преобразованный массив. 
def n3(a:array):
    for i in range(8):
        if a[i] < 15:
            a[i] *= 2
    print(a[i])

# №4 Дан одномерный массив целого типа. Получить другой массив, состоящий только из нечетных чисел исходного массива или сообщить, что таких чисел нет. Полученный массив вывести в порядке убывания элементов.
def n4(a:array):
    odd_numbers = []
    for num in a:
        if num % 2 != 0:
            odd_numbers.append(num)
    
    if not odd_numbers:
        print("Нечетных чисел в массиве нет")
    else:
        # Сортировка по убыванию
        odd_numbers.sort(reverse=True)
        print(f"Массив нечетных чисел (по убыванию): {odd_numbers}")

# №5 Дан целочисленный массив размера 10. Создать новый массив, удалив все одинаковые элементы, оставив их 1 раз.
def n5(a:array): 
    unique_a = []
    for num in a:
        if num not in unique_arr:
            unique_a.append(num)
    
    print(f"Массив без дубликатов: {unique_a}")

# №6 Одномерный массив из 10-и целых чисел заполнить с клавиатуры, определить сумму тех чисел, которые >5. 
def n6(a:array):
     total = 0
     for num in a:
        if num > 5:
            total += num
     print(f"Сумма чисел больше 5: {total}")

# №7 Переставить в одномерном массиве минимальный элемент и максимальный
def n7(a:array,n:int):
    if n >= 2:
        min_index = 0
        max_index = 0
        
        for i in range(1, n):
            if a[i] < a[min_index]:
                min_index = i
            if a[i] > a[max_index]:
                max_index = i
        
        a[min_index], a[max_index] = a[max_index], a[min_index]
        
        print(f"Минимальный элемент был на позиции {min_index}")
        print(f"Максимальный элемент был на позиции {max_index}")
        print(f"Массив после перестановки: {a}")
    else:
        print("Массив слишком мал для перестановки")
 
# №8 В массиве действительных чисел все нулевые элементы заменить на среднее арифметическое всех элементов массива.
def n8(a:array,n:int):
    total = sum(a)
    average = total / n if n > 0 else 0
    
    # Замена нулевых элементов
    for i in range(n):
        if a[i] == 0:
            a[i] = average
    
    print(f"Среднее арифметическое: {average}")
    print(f"Преобразованный массив: {a}")
   
# №9 Даны массивы A и B одинакового размера 10. Вывести исходные массивы. Поменять местами их содержимое и вывести в начале элементы преобразованного массива A, а затем — элементы преобразованного массива B.
def n9(a:array,b:array):
     A, B = B, A
    
     print(f"\nПосле обмена:")
     print(f"Массив A: {A}") 
     print(f"Массив B: {B}")

# №10 Дан одномерный массив из 15 элементов. Элементам массива меньше 10 присвоить нулевые значения, а элементам больше 20 присвоить 1. Вывести на экран монитора первоначальный и преобразованный массивы в строчку.
def n10(a:array):
    b=[]
    for num in a:
        if num < 10:
            b.append(0)
        elif num > 20:
            b.append(1)
        else:
            b.append(num)
    print(f"Исходный массив: {a}\nПреобразованный массив: {b}")

# №11 Дан одномерный массив целого типа. Получить другой массив, состоящий только из четных чисел исходного массива, меньше 10, или сообщить, что таких чисел нет.
def n11(a:array):
     result = []
     for num in arr:
        if num % 2 == 0 and num < 10:
            result.append(num)
     if not result:
        print("Четных чисел меньше 10 в массиве нет")
     else:
        result.sort()
        print(f"Четные числа меньше 10 (по возрастанию): {result}")

# №12 Даны массивы A и B одинакового размера 10. Поменять местами их содержимое и вывести вначале элементы преобразованного массива A, а затем — элементы преобразованного массива B.
def n12(a:array,b:array):
     A, B = B, A
    
     print(f"\nМассив A после обмена: {A}")
     print(f"Массив B после обмена: {B}")

# №13 Дан одномерный массив из 8 элементов. Заменить все элементы массива меньшие 15 их удвоенными значениями.
def n13(a:array):
    n3(a)

# №14 Программа заполняет одномерный массив из 10 целых чисел числами, считанными с клавиатуры. Определить среднее арифметическое всех чисел массива. Заменить элементы массива большие среднего арифметического на 1.
def n14(a:array):
    total = sum(a)
    average = total / 10
    
    print(f"Среднее арифметическое: {average}")
    
    # Замена элементов больше среднего
    for i in range(10):
        if a[i] > average:
            a[i] = 1
    
    print(f"Преобразованный массив: {a}")

# №15 Дан одномерный массив целого типа. Получить другой массив, состоящий только из нечетных чисел исходного массива или сообщить, что таких чисел нет. Полученный массив вывести в порядке убывания элементов.
def n15(a:array):
    n4(a)